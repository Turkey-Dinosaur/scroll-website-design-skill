# Hero frame sequence — JPG ~250 KB each, ~33 MB total. Small enough
# to stay in regular git (no LFS needed). Previously PNGs in LFS;
# converted to JPG q=4 in commit "perf: convert hero frame sequence
# from PNG to JPG". Cloners no longer need `git lfs pull`.

# Normalise text file line endings.
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.md   text eol=lf
*.svg  text eol=lf
